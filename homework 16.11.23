#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main() {
    def draw_square(side) :
        for i in range(side) :
            for j in range(side) :
                if i == 0 or i == ```
    def draw_square(side) :
        for i in range(side) :
            for j in range(side) :
                if i == 0 or i == def draw_square(side) :
        for i in range(side) :
            for j in range(side) :
                if i == 0 or i == side - 1 or j == 0 or j == side - 1 :
                      print("*", end = " ")
                else:
                      print(" ", end = " ")
         print()
     side = int(input("Введите длину стороны квадрата: "))
     draw_square(side)
}
